defaults:
  system:
    service:
      rsyslog:
        disable: true
      netplan:
        testif:
          match:
            macaddress: "de:ad:be:ef:ca:fe"
          set-name: testif
          dhcp: true
